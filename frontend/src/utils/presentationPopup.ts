/**
 * @deprecated - Le popup natif est trop souvent bloqué par les navigateurs.
 * Utiliser `PresentationPanel` (composant React) + `presentationLoader` à la place.
 * Ce fichier est laissé vide pour préserver la compatibilité de build.
 */
export {};
